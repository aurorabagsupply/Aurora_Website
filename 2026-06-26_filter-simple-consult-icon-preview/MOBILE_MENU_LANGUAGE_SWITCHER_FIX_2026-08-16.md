# Mobile Menu Language Switcher Fix - 2026-08-16

- Added a visible globe language icon to the Home row of the expanded mobile menu.
- Kept the existing language choices: EN, 中文, RU, ES, FR, DE, IT, AR.
- Hardened initialization so the icon is inserted after the mobile menu is rendered.
- Added mobile-only styling for the icon and language list without changing desktop layout.
- Updated static and WordPress asset versions to avoid stale browser cache.
- Validation: JavaScript syntax check and Git whitespace check passed.

The GitHub Pages preview and the live WordPress site are separate. The preview URL must include the versioned project path; the WordPress site needs the updated theme package before the live customer site changes.

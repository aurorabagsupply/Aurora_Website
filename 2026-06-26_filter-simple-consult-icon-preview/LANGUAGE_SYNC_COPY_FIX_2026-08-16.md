# Language Sync Copy Fix - 2026-08-16

- Added localized Hero category actions so the Bag slide uses natural labels such as `View Bag` and `查看包袋` instead of a raw category label.
- Added the missing `Quality` translation key for all supported languages.
- Updated the shared navigation refresh so every `quality.html` link, including the mobile menu and footer, follows the active language.
- Bumped static and WordPress asset versions to prevent stale mobile JavaScript from being served.

# Mobile Menu Outside-Close Update

- Fixed the mobile navigation drawer so tapping the blank area outside the menu panel closes it and restores the underlying page.
- Kept menu links, submenu toggles, desktop behavior, and existing navigation logic unchanged.
- Synchronized the static preview and WordPress theme scripts.
